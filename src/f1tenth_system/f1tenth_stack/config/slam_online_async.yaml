# Mapping-specific overrides. Shared params live in slam_common.yaml;
# slam_launch.py loads both (common first, this file second).
slam_toolbox:
  ros__parameters:
    use_map_saver: true
    mode: mapping

    # Loop closure (on for mapping)
    do_loop_closing: true
    loop_search_maximum_distance: 4.0
    loop_match_minimum_chain_size: 5
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.3
    loop_match_minimum_response_fine: 0.4

    # Loop closure correlation
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
